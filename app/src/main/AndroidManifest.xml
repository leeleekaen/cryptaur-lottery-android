<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cryptaur.lottery">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".TheApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="e828e102371d5b2c13b24042d9c543fb0dcce000" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MyTicketsActivity"
            android:label="@string/title_activity_my_tickets"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".buytickets.BuyTicketActivity"
            android:label="@string/title_activity_buy_ticket"
            android:theme="@style/AppTheme.Light" />
    </application>

</manifest>